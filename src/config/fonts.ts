import { Lato, Tourney, Roboto } from 'next/font/google'

export const lato = Lato({ subsets: ['latin'], weight: ['400'] })

export const tourney = Tourney({ subsets: ['latin'], weight: ['900'] })

export const roboto = Roboto({ subsets: ['latin'], weight: ['700'] })
